const ndarray_ = require('./ndarray');
const ndvertex_ = require('./ndvertex');
const ndedge_ = require('./ndedge');

module.exports = {
    ndarray: ndarray_,
    ndvertex: ndvertex_,
    ndedge: ndedge_,
}